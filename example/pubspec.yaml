name: example
description: A new Flutter application.
version: 1.0.0+1
environment:
  sdk: ">=2.8.0 <3.0.0"
  flutter: ">=1.17.0 <2.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_menu:
    path: ..
  collection:
  cupertino_icons:
  disposable_provider:
  enum_to_string:
  freezed_annotation:
  json_annotation:
  mono_kit: ^0.16.0-dev
  provider:
  rxdart:
  vsync_provider:
  flutter_portal:
dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  freezed:
  json_serializable:
  pedantic_mono:
flutter:
  uses-material-design: true
#  assets:
#    - assets/images/
